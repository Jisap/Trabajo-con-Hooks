import { createContext } from 'react';

export const UserContext = createContext(null); //createContext crea un objeto context -> Higher object component
                                                //Podemos colocarle componentes hijos.
